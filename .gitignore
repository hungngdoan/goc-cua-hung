node_modules/
dist/
.astro/

.DS_Store
Thumbs.db
*.local
*.log
.env
.env.*

# Font binary used only by scripts/make-share-card.py (download URL in its header)
scripts/*.ttf

# Output of `npm run check:sections`. One run writes ~11 MB of PNGs; only the
# committed baseline in scripts/ is meant to be tracked.
.section-check/
